# Contributing To SqrLatt

Keep geometry and material parameters in each case config. Keep resolution,
grid, Berry step, EFS settings, symmetry mode, and plot settings in their task
scripts. Do not add local `.pkl` records or generated images to Git.

Changes to shared geometry, k-space conventions, or record schemas belong in
MePhC and should identify the MePhC tag or commit they require.
